# The version of the Kubernetes API to use
apiVersion: v1
# What kind of resource we are creating (ConfigMap stores non-confidential data)
kind: ConfigMap
metadata:
  # The name of this ConfigMap
  name: hello-node-config
data:
  # The actual data we want to store
  ENV_VALUE: "Hello from k8s via ConfigMap!"
