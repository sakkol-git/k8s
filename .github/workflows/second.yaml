name: Second CI

on: 
    push:
        branches: [ "main", "master" ]
    pull_request:
        branches: [ "main", "master" ]

jobs:
    build:
        runs-on: ubuntu-latest
        steps: 
            - name: Say Hello
              run: echo "hello from second workflow"
        